// DOM JS methods:

// documentElement - returns the root of the document
// document.body - returns the body element
// using d. from here on
// d.childNodes
// d.firstChild
// d.lastChild
// d.previousSibling
// d.nextSibling
//
// nodeType property returns what kind of element it is:
// d.ELEMENT_NODE - value 1
// d.TEXT_NODE - value 3
// d.COMMENT_NODE - value 8

// finding elements
// all of these are "live" selectors
// d.getElementById
// d.getElementsByClassName
// d.getElementsByTagName
// d.querySelectorAll(selector) - finds all elements by query
// d.querySelector(selector) - finds first by query

// making elements
// d.createTextNode
// d.createElement(type)
// d.appendChild - puts it at the end of the list of children
// d.replaceChild(newNode, replaceNode) - replace a node w/ another one
// d.insertBefore(node, insertBefore) - puts node before the insertBefore node

// requestAnimationFrame(<func>)
